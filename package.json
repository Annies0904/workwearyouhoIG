{
  "name": "workwearyouhoig",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:webhook": "npm --workspace=packages/webhook run dev",
    "dev:admin": "npm --workspace=packages/admin run dev",
    "build:admin": "npm --workspace=packages/admin run build"
  }
}
