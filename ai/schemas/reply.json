{
  "name": "ig_reply",
  "description": "Structured reply generated by the IG assistant",
  "strict": true,
  "schema": {
    "type": "object",
    "properties": {
      "reply": {
        "type": "string",
        "description": "The message text to send back to the Instagram user"
      },
      "confidence": {
        "type": "number",
        "description": "Confidence score between 0 and 1. Values below 0.6 trigger human-review flag.",
        "minimum": 0,
        "maximum": 1
      },
      "needs_human": {
        "type": "boolean",
        "description": "True if the message should be escalated to a human agent"
      },
      "category": {
        "type": "string",
        "description": "Topic category of the customer question",
        "enum": ["sizing", "shipping", "returns", "product", "promotion", "general", "out_of_scope"]
      }
    },
    "required": ["reply", "confidence", "needs_human", "category"],
    "additionalProperties": false
  }
}
